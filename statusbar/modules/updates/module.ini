type = custom/script

click-left=${TERMINAL:-st} sudo pacman -Syyu
exec = checkupdates 2>/dev/null | wc -l || echo 0
label=" %{F-} %output% updates "
label-background = ${colors.bg-alt}
label-foreground = ${colors.color1}
label-underline = ${colors.color1}
interval=600
